# 🚀 Быстрый старт Outlivion Backend

## Шаг 1: Установка зависимостей
```bash
npm install
```

## Шаг 2: Настройка базы данных

### Создайте базу данных PostgreSQL:
```bash
# Если используете локальный PostgreSQL
createdb outlivion_db

# Или через psql:
psql -U postgres -c "CREATE DATABASE outlivion_db;"
```

### Настройте DATABASE_URL в .env:
```env
DATABASE_URL=postgresql://username:password@localhost:5432/outlivion_db
```

## Шаг 3: Применение миграций

```bash
# Генерация миграций (уже выполнено)
npm run db:generate

# Применение миграций
npm run db:migrate
```

## Шаг 4: Запуск сервера

```bash
npm run dev
```

Сервер будет доступен на http://localhost:3001

## 📝 Проверка работы

```bash
# Проверка здоровья сервера
curl http://localhost:3001/health
```

## 🔧 Полезные команды

```bash
# Открыть Drizzle Studio (визуальный редактор БД)
npm run db:studio

# Применить изменения схемы без миграций (только для разработки)
npm run db:push

# Сборка для продакшена
npm run build
npm start
```

## ⚠️ Важно

1. Убедитесь, что PostgreSQL запущен
2. Проверьте правильность DATABASE_URL в .env
3. Для продакшена измените JWT_SECRET на безопасный ключ


